# MLflow

This service runs the Astrixa local MLflow tracking backend.

Responsibilities:

- persist run metadata for gateway requests
- expose the MLflow UI and tracking API
- provide a local development backend for request-level tracing artifacts
