# API Docs

This directory will contain versioned API contracts for:

- API Gateway
- Provider Registry
- Agent Registry
- internal service contracts where versioning matters

The target is contract-first development, not ad hoc endpoint drift.

